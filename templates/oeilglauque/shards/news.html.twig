<div class="well">
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('editNews', {'slug': slug}) }}">Éditer</a>
    {% endif %}
    <a href="/news/{{ slug }}"><h2 class="redandblack">{{ title }}</h2></a>
    <p>{{ text }}</p>
</div>