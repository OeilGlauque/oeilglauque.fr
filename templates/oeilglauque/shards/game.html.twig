<div class="row">
    <div class="col-lg-12">
        <div class="well">
            <a href="/partie/{{ id }}"><h2 class="redandblack">{{ title }}</h2></a>
            <p class="small">Proposé par {{ author }}<br />
            Créneau : {{ slot }}</p>
            <p>{{ text }}</p>
            <br />
            {% if freeSeats > 0 %}
            <p>{{freeSeats}} places disponibles</p>
            {% else %}
            <p>Malheureusement, il n'y a plus de places disponible...
            {% if onlineOnly == false %}
            Mais vous pourrez encore vous inscrire à l'accueil du festival !
            {% endif %}
            </p>
            {% endif %}
        </div>
    </div>
</div><br />