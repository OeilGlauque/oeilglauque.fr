{% extends 'base.html.twig' %}

{% block body %}

        <main role="main" class="container">
            <h1>{{ news.getTitle() }}</h1>
            <div class="row">
                <div class="col-lg-12">
                    <p>{{ news.getText() }}</p><br />
                    <a href="{{ path('newsIndex') }}">Retour aux news</a>
                </div>
            </div>
        </main><!-- /.container -->

{% endblock %}